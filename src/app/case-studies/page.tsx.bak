import Navbar from "@/components/Navbar";
import Footer from "@/components/public/Footer";
import { ArrowUpRight } from "lucide-react";
import Link from "next/link";

const studies = [
  { company: "TechFlow", metric: "+450%", desc: "Increase in organic traffic within 6 months via programmatic SEO builds.", tag: "SEO Mastery" },
  { company: "StyleStore", metric: "$2M+", desc: "Revenue generated from TikTok Shop affiliates in Q4.", tag: "TikTok Commerce" },
  { company: "HealthVite", metric: "-60%", desc: "Reduction in Customer Acquisition Cost through advanced Lookalike modeling.", tag: "Social Advertising" },
  { company: "SaaSify", metric: "600ms", desc: "Global First Input Delay achieved after Next.js architecture overhaul.", tag: "Web Engineering" }
];

export default function CaseStudiesPage() {
  return (
    <main className="min-h-screen bg-slate-950 text-white flex flex-col">
      <Navbar />
      
      <section className="pt-32 pb-20 bg-slate-900 border-b border-slate-800">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h1 className="text-5xl md:text-7xl font-bold mb-6 tracking-tight">Our Work.</h1>
          <p className="text-xl text-slate-400 max-w-2xl leading-relaxed">
            Case studies detailing exactly how we've scaled our partners using proprietary growth frameworks. Real numbers, real impact.
          </p>
        </div>
      </section>

      <section className="py-24 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex-1">
        <div className="grid md:grid-cols-2 gap-8">
          {studies.map((study, i) => (
            <div key={i} className="group bg-slate-950 border border-slate-800 rounded-3xl p-10 hover:border-yellow-400/50 transition-colors cursor-pointer">
              <div className="flex justify-between items-start mb-8">
                <span className="bg-slate-900 px-3 py-1 rounded-full text-xs font-semibold text-slate-300">{study.tag}</span>
                <ArrowUpRight className="text-slate-600 group-hover:text-yellow-400 transition-colors w-6 h-6" />
              </div>
              <h2 className="text-6xl font-bold text-yellow-400 mb-4">{study.metric}</h2>
              <h3 className="text-2xl font-bold text-white mb-2">{study.company}</h3>
              <p className="text-slate-400 leading-relaxed border-t border-slate-800 pt-6 mt-6">
                {study.desc}
              </p>
            </div>
          ))}
        </div>
      </section>

      <Footer />
    </main>
  );
}
